<?xml version="1.0" encoding="utf-8"?>
<menu
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
            android:id="@+id/update_member_list_item"
            android:icon="@drawable/ic_update"
            android:orderInCategory="100"
            android:title="@string/update_text"
            app:showAsAction="ifRoom"/>

    <item
            android:id="@+id/confirm_member_list_item"
            android:icon="@drawable/ic_confirm"
            android:orderInCategory="100"
            android:title="@string/confirm_text"
            app:showAsAction="ifRoom"/>
    <item
            android:id="@+id/item_settings"
            android:icon="@drawable/ic_settings"
            android:orderInCategory="100"
            android:title="@string/settings_text"
            app:showAsAction="always"/>

</menu>